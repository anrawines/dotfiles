;
;	███╗   ███╗ ██████╗ ██████╗ ██╗   ██╗██╗     ███████╗███████╗
;	████╗ ████║██╔═══██╗██╔══██╗██║   ██║██║     ██╔════╝██╔════╝
;	██╔████╔██║██║   ██║██║  ██║██║   ██║██║     █████╗  ███████╗
;	██║╚██╔╝██║██║   ██║██║  ██║██║   ██║██║     ██╔══╝  ╚════██║
;	██║ ╚═╝ ██║╚██████╔╝██████╔╝╚██████╔╝███████╗███████╗███████║
;	╚═╝     ╚═╝ ╚═════╝ ╚═════╝  ╚═════╝ ╚══════╝╚══════╝╚══════╝
;                                                             
;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/alsa]
type = internal/alsa

; Soundcard to be used
; Usually in the format hw:# where # is the card number
; You can find the different card numbers in `/proc/asound/cards`
master-soundcard = default
speaker-soundcard = default
headphone-soundcard = default

; Name of the master, speaker and headphone mixers
; Use the following command to list available mixer controls:
; $ amixer scontrols | sed -nr "s/.*'([[:alnum:]]+)'.*/\1/p"
; If master, speaker or headphone-soundcard isn't the default, 
; use `amixer -c # scontrols` instead where # is the number 
; of the master, speaker or headphone soundcard respectively
;
; Default: Master
master-mixer = Master

; Optionally define speaker and headphone mixers
; Default: none
;;speaker-mixer = Speaker
; Default: none
;;headphone-mixer = Headphone

; NOTE: This is required if headphone_mixer is defined
; Use the following command to list available device controls
; $ amixer controls | sed -r "/CARD/\!d; s/.*=([0-9]+).*name='([^']+)'.*/printf '%3.0f: %s\n' '\1' '\2'/e" | sort
; You may also need to use `amixer -c # controls` as above for the mixer names
; Default: none
;;headphone-id = 9

; Use volume mapping (similar to amixer -M and alsamixer), where the increase in volume is linear to the ear
; Default: false
;;mapped = true

; Interval for volume increase/decrease (in percent points)
; Default: 5
interval = 5

; Available tags:
;   <label-volume> (default)
;   <ramp-volume>
;   <bar-volume>
; format-volume = <ramp-volume><label-volume><bar-volume>
format-volume = <ramp-volume><label-volume>
format-volume-background = ${color.bg}
format-volume-underline = ${color.brown}

; Available tags:
;   <label-muted> (default)
;   <ramp-volume>
;   <bar-volume>
format-muted = <label-muted>
format-muted-prefix = " 婢 "
format-muted-prefix-font = 3
format-muted-prefix-underline = ${color.brown}
format-muted-underline = ${color.brown}
format-muted-foreground = ${color.fg}
format-muted-background = ${color.bg}

; Available tokens:
;   %percentage% (default)
label-volume = "%percentage%% "
label-volume-foreground = ${color.fg}
label-volume-background = ${color.bg}
format-volume-padding = 0

bar-volume-width = 5
bar-volume-foreground = ${color.brown}
bar-volume-fill = "━"
bar-volume-fill-foreground = ${color.brown}
bar-volume-fill-font = 3
bar-volume-fill-background = ${color.bg}

bar-volume-empty = "━"
bar-volume-empty-font = 3
bar-volume-empty-foreground = ${color.brown}
bar-volume-empty-background = ${color.bg}

bar-volume-indicator = 
bar-volume-indicator-foreground = ${color.brown}
bar-volume-indicator-font = 3



; Available tokens:
;   %percentage% (default
label-muted = " Muted " 
label-muted-foreground = ${color.red}

; Only applies if <ramp-volume> is used
ramp-volume-0 = "  "
ramp-volume-1 = "  "
ramp-volume-2 = " 󰕾 "
ramp-volume-foreground = ${color.pink}
ramp-volume-background = ${color.bg}
ramp-volume-font = 1

; If defined, it will replace <ramp-volume> when
; headphones are plugged in to `headphone_control_numid`
; If undefined, <ramp-volume> will be used for both
; Only applies if <ramp-volume> is used
ramp-headphones-0 = ""
ramp-headphones-1 = ""

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/backlight]
type = internal/xbacklight

; Use the following command to list available cards:
; $ ls -1 /sys/class/backlight/
;card = intel_backlight
card = acpi_video0

enable-scroll = true
use-actual-brightness = true

; Available tags:
;   <label> (default)
;   <ramp>
;   <bar>
format = <ramp><label>
format-background = ${color.BG}

; Available tokens:
;   %percentage% (default)
label = "%percentage%%"
label-foreground = ${color.FG}

; Only applies if <ramp> is used

ramp-0 = " "
ramp-1 = " "
ramp-2 = " "
ramp-3 = " "
ramp-4 = " "
ramp-5 = " "
ramp-6 = " "
ramp-7 = " "
ramp-font = 1
ramp-foreground = ${color.BROWN}

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/battery]
type = internal/battery

; This is useful in case the battery never reports 100% charge
full-at = 99
low-at = 30

; Use the following command to list batteries and adapters:
; $ ls -1 /sys/class/power_supply/
battery = BAT0
adapter = ADP0

; If an inotify event haven't been reported in this many
; seconds, manually poll for new values.
;
; Needed as a fallback for systems that don't report events
; on sysfs/procfs.
;
; Disable polling by setting the interval to 0.
;
; Default: 5
poll-interval = 5

; see "man date" for details on how to format the time string
; NOTE: if you want to use syntax tags here you need to use %%{...}
; Default: %H:%M:%S
time-format = %H:%M

; Available tags:
;   <label-charging> (default)
;   <bar-capacity>
;   <ramp-capacity>
;   <animation-charging>
format-charging = <label-charging><animation-charging>
format-charging-foreground = ${color.fg}
format-charging-prefix = "  "
format-charging-prefix-font = 4
format-charging-prefix-foreground = #145314
format-charging-background = ${color.bg}
; Available tags:
;   <label-discharging> (default)
;   <bar-capacity>
;   <ramp-capacity>
;   <animation-discharging>
;format-discharging = <ramp-capacity><label-discharging>
format-discharging = <label-discharging>
format-discharging-foreground = ${color.fg}
format-discharging-prefix = " 󰚦"
format-discharging-prefix-font = 4
format-discharging-prefix-foreground = #7e002f
format-discharging-background = ${color.bg}

; Available tags:
;   <label-full> (default)
;   <bar-capacity>
;   <ramp-capacity>
format-full = <label-full>
format-full-foreground = ${color.fg}
;format-full-underline = ${color.fg}
;format-full-prefix-underline = ${color.red}
format-full-prefix = " 󱊦 "
format-full-prefix-font = 4
format-full-prefix-foreground = #1D9A6C
format-full-background = ${color.bg}

; Available tokens:
;   %percentage% (default)
;   %time%
;   %consumption% (shows current charge rate in watts)
label-charging = "%percentage:3:3%% "
label-charging-background = ${color.bg}

; Available tokens:
;   %percentage% (default)
;   %time%
;   %consumption% (shows current discharge rate in watts)
label-discharging = "%percentage:3:3%% " 
label-discharging-background = ${color.bg}

; Available tokens:
;   %percentage% (default)
label-full = "%percentage:3:3%% "
 


; Only applies if <ramp-capacity> is used
ramp-capacity-0 = "  "
ramp-capacity-1 = "  "
ramp-capacity-2 = "  "
ramp-capacity-3 = "  "
ramp-capacity-4 = "  "
;ramp-capacity-foreground = #145314
;ramp-capacity-background = ${color.BG}
ramp-capacity-font = 1

; Only applies if <bar-capacity> is used
;bar-capacity-width = 10

; Only applies if <animation-charging> is used
animation-charging-0 = "  "
animation-charging-1 = "  "
animation-charging-2 = "  "
animation-charging-3 = "  "
animation-charging-4 = "  "
animation-charging-foreground = ${color.fg}
animation-charging-background = ${color.bg}
animation-charging-font = 1

; Framerate in milliseconds
animation-charging-framerate = 700

; Only applies if <animation-discharging> is used
;;animation-discharging-0 = ${battery.anim0}
;;animation-discharging-1 = ${battery.anim1}

; Framerate in milliseconds
;animation-discharging-framerate = 500

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/cpu]
type = internal/cpu

; Seconds to sleep between updates
; Default: 1
interval = 3

; Available tags:
;   <label> (default)
;   <bar-load>
;   <ramp-load>
;   <ramp-coreload>
;;format = <label> <ramp-coreload>
format = <label>
format-prefix = "  "
format-prefix-font = 1
;format-underline = ${color.green}
;format-prefix-underline = ${color.green}
format-prefix-foreground = ${color.green}
format-background = ${color.bg}

; Available tokens:
;   %percentage% (default) - total cpu load averaged over all cores
;   %percentage-sum% - Cumulative load on all cores
;   %percentage-cores% - load percentage for each core
;   %percentage-core[1-9]% - load percentage for specific core
;label = "%percentage:2%% "
label = "%percentage:2:2:%% "
label-foreground = ${color.fg}

; Spacing between individual per-core ramps
ramp-coreload-spacing = 1
ramp-coreload-0 = " "
ramp-coreload-1 = " "
ramp-coreload-2 = " "
ramp-coreload-3 = " "
ramp-coreload-4 = " "

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_


[module/date]
type = internal/date
interval = 60
;date = %{F#6782ED}%d %b%{F-} %{F#d0d7de}%H:%M %p%{F-}
date = %{F#d0d7de}%a , %H:%M %p%{F-}
;date = %{T3} %H:%M -%{T1} %A %d.%m
;date-alt = %{F#d0d7de}%a/%d/%b%{F-} - %{F#d0d7de}%H:%M %p %{F-}
label = %date%
format = <label>
format-background = ${color.bg}
format-foreground = ${color.fg}
format-padding = 1
;format-prefix = " " 
;format-prefix-font = 2
;format-prefix-foreground = ${color.fg}
;format-prefix-background = ${color.teal}
date-alt = ~/.config/i3/polybar/themes/slim-colorfull/popup-calendar.sh --popup &


[module/dates]
type = internal/date

; Seconds to sleep between updates
; Default: 1.0
interval = 1.0

; See "https://en.cppreference.com/w/cpp/io/manip/put_time" for details on how to format the date string
; NOTE: if you want to use syntax tags here you need to use %%{...}
date = %Y-%m-%d%

; Optional time format
time = %H:%M

; if `date-alt` or `time-alt` is defined, clicking
; the module will toggle between formats
date-alt = %A, %d %B %Y
time-alt = %H:%M:%S

; Available tags:
;   <label> (default)
format = <label>
format-background = #555
format-foreground = ${color.fg}

; Available tokens:
;   %date%
;   %time%
; Default: %date%
label = %date% %time%
label-font = 3
label-foreground = ${color.fg}

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/filesystem]
type = internal/fs

; Mountpoints to display
mount-0 = /home
mount-1 = /home
mount-2 = /var

; Seconds to sleep between updates
; Default: 30
interval = 30

; Display fixed precision values
; Default: false
fixed-values = false

; Spacing between entries
; Default: 2
;;spacing = 4

; Available tags:
;   <label-mounted> (default)
;   <bar-free>
;   <bar-used>
;   <ramp-capacity>
format-mounted = <label-mounted>
format-mounted-background = ${color.BG1}

format-mounted-prefix = 
format-mounted-prefix-font = 2
format-mounted-prefix-foreground = ${color.ORANGE}

; Available tags:
;   <label-unmounted> (default)
format-unmounted = <label-unmounted>
format-unmounted-background = ${color.BG1}

format-unmounted-prefix = 
format-unmounted-prefix-font = 2
format-unmounted-prefix-foreground = ${color.ORANGE}

; Available tokens:
;   %mountpoint%
;   %type%
;   %fsname%
;   %percentage_free%
;   %percentage_used%
;   %total%
;   %free%
;   %used%
; Default: %mountpoint% %percentage_free%%
label-mounted = "%percentage_free%%"

; Available tokens:
;   %mountpoint%
; Default: %mountpoint% is not mounted
label-unmounted = "%mountpoint%"

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/memory]
type = internal/memory

; Seconds to sleep between updates
; Default: 1
interval = 3

; Available tags:
;   <label> (default)
;   <bar-used>
;   <bar-free>
;   <ramp-used>
;   <ramp-free>
;   <bar-swap-used>
;   <bar-swap-free>
;   <ramp-swap-used>
;   <ramp-swap-free>
format = <label>
format-background = ${color.bg}

format-prefix = "﬙"
format-prefix-font = 3
format-prefix-foreground = ${color.blue}

; Available tokens:
;   %percentage_used% (default)
;   %percentage_free%
;   %gb_used%
;   %gb_free%
;   %gb_total%
;   %mb_used%
;   %mb_free%
;   %mb_total%
;   %percentage_swap_used%
;   %percentage_swap_free%
;   %mb_swap_total%
;   %mb_swap_free%
;   %mb_swap_used%
;   %gb_swap_total%
;   %gb_swap_free%
;   %gb_swap_used%

label = " %percentage_used%"

; Only applies if <ramp-used> is used
;;ramp-used-0 = 
;;ramp-used-1 = 
;;ramp-used-2 = 
;;ramp-used-3 = 
;;ramp-used-4 = 

; Only applies if <ramp-free> is used
;;ramp-free-0 = 
;;ramp-free-1 = 
;;ramp-free-2 = 
;;ramp-free-3 = 
;;ramp-free-4 = 

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/mpd]
type = internal/mpd

; Host where mpd is running (either ip or domain name)
; Can also be the full path to a unix socket where mpd is running.
;;host = 127.0.0.1
;;port = 6600
;;password = mysecretpassword

; Seconds to sleep between progressbar/song timer sync
; Default: 1
interval = 2

; Available tags:
;   <label-song> (default)
;   <label-time>
;   <bar-progress>
;   <toggle> - gets replaced with <icon-(pause|play)>
;   <toggle-stop> - gets replaced with <icon-(stop|play)>
;   <icon-random>
;   <icon-repeat>
;   <icon-repeatone> (deprecated)
;   <icon-single> - Toggle playing only a single song. Replaces <icon-repeatone>
;   <icon-consume>
;   <icon-prev>
;   <icon-stop>
;   <icon-play>
;   <icon-pause>
;   <icon-next>
;   <icon-seekb>
;   <icon-seekf>

format-online =   <label-song>  |  <icon-prev> <toggle> <icon-stop> <icon-next>
;;format-online-prefix = " "
;;format-online-prefix-font = 2
;;format-online-prefix-foreground = ${color.GREEN}

format-playing = ${self.format-online}
format-playing-background = ${color.bg}
format-playing-padding = 2
format-paused = ${self.format-online}
format-paused-background = ${color.bg}
format-paused-padding = 2
format-stopped = <icon-prev> <toggle> <icon-next>
format-stopped-background = ${color.bg}
format-stopped-padding = 2

; Available tags:
;   <label-offline>
format-offline = <label-offline>
label-offline-background = ${color.bg}
label-offline-padding = 1
format-offline-prefix = ""
format-offline-prefix-font = 1
format-offline-prefix-background = ${color.bg}
format-offline-prefix-foreground = ${color.pink}
format-offline-prefix-padding = 1

; Available tokens:
;   %artist%
;   %album-artist%
;   %album%
;   %date%
;   %title%
; Default: %artist% - %title%
label-song =  "%title%"
label-song-maxlen = 13
label-song-ellipsis = true

; Available tokens:
;   %elapsed%
;   %total%
; Default: %elapsed% / %total%
;;abel-time = %elapsed% / %total%

; Available tokens:
;   None
label-offline = "Offline"

; Only applies if <icon-X> is used
icon-play = ""
icon-play-font = 4
icon-play-foreground = ${color.green}
icon-pause = ""
icon-pause-font = 4
icon-pause-foreground = ${color.yellow}
icon-stop = ""
icon-stop-font = 4
icon-stop-foreground = ${color.red}
icon-prev = "󰙣" 
icon-prev-font = 1
icon-prev-foreground = ${color.cyan}
icon-next = "󰙡" 
icon-next-font = 1
icon-next-foreground = ${color.cyan}

; Used to display the state of random/repeat/repeatone/single
; Only applies if <icon-[random|repeat|repeatone|single]> is used
;;toggle-on-foreground = #ff
;;toggle-off-foreground = #55

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[network-base]
type = internal/network
interval = 5
format-connected = <label-connected>
format-disconnected = <label-disconnected>
label-disconnected = "Null "

ping-interval = 3
format-packetloss = <animation-packetloss> <label-connected>

; Only applies if <animation-packetloss> is used
animation-packetloss-0 = ⚠
animation-packetloss-0-foreground = #ffa64c
animation-packetloss-1 = 📶
animation-packetloss-1-foreground = #000000
; Framerate in milliseconds
animation-packetloss-framerate = 500

; If you use both a wired and a wireless network, just add 2 module definitions. For example
[module/wired-network]
inherit = network-base
interface-type = wired
format-label-disconnected-prefix = %{F#F0C674} 󰈂 %{F-}
format-disconnected-background = ${color.bg}

label-connected = "%{F#765fa2} 󰈀 %{F-} %upspeed:3:6:% %downspeed:3:6:% "
label-connected-background = ${color.bg}

[module/wireless-network]
inherit = network-base
interface-type = wireless
format-disconnected-prefix =  %{F#bb9af7} 󰤭 %{F-}
;label-disconnected = %{F#bb9af7} 󰤭 %{F-}
format-disconnected-background = ${color.bg}

label-connected = "%{F#bb9af7}  %{F-} %upspeed:5:5:% %downspeed:5:5:% "
label-connected-background = ${color.bg}

[module/network]
type = internal/network
interface = wlan0

; Seconds to sleep between updates
; Default: 1
interval = 1.0

; Test connectivity every Nth update
; A value of 0 disables the feature
; NOTE: Experimental (needs more testing)
; Default: 0
ping-interval = 3

; @deprecated: Define min width using token specifiers (%downspeed:min% and %upspeed:min%)
; Minimum output width of upload/download rate
; Default: 3
;;udspeed-minwidth = 5

; Accumulate values from all interfaces
; when querying for up/downspeed rate
; Default: false
accumulate-stats = true

; Consider an `UNKNOWN` interface state as up.
; Some devices have an unknown state, even when they're running
; Default: false
unknown-as-up = true

; Available tags:   <label-connected> (default)
;   <ramp-signal>
format-connected = <ramp-signal><label-connected>
; format-connected = <ramp-signal>
format-connected-underline = ${color.brown}
format-connected-foreground = ${color.brown}
format-connected-background = ${color.bg}
format-connected-padding = 0

; Available tags:
;   <label-disconnected> (default)
format-disconnected = <label-disconnected>
format-disconnected-prefix = " "
format-disconnected-prefix-font = 1
format-disconnected-prefix-foreground = ${color.brown}
format-disconnected-prefix-background = ${color.bg}
format-disconnected-foreground = ${color.brown}
format-disconnected-background = ${color.bg}
format-disconnected-padding = 1

; Available tags:
;   <label-connected> (default)
;   <label-packetloss>
;   <animation-packetloss>
;;format-packetloss = <animation-packetloss> <label-connected>

; Available tokens:
;   %ifname%    [wireless+wired]
;   %local_ip%  [wireless+wired]
;   %local_ip6% [wireless+wired]
;   %essid%     [wireless]
;   %signal%    [wireless]
;   %upspeed%   [wireless+wired]
;   %downspeed% [wireless+wired]
;   %linkspeed% [wired]
; Default: %ifname% %local_ip%
; label-connected = "%{A1:networkmanager_dmenu &:}%essid% %{A}"
label-connected = "%{A1:networkmanager_nm-applet &:}%essid%  %upspeed% "
label-connected-underline = ${color.white}
label-connected-foreground = ${color.fg}
label-connected-background = ${color.bg}

; Available tokens:
;   %ifname%    [wireless+wired]
; Default: (none)
label-disconnected = "%{A1:networkmanager_nm-applet &:}Offline%{A}"
label-disconnected-foreground = ${color.fg}
label-disconnected-background = ${color.bg}
;;label-disconnected-foreground = #66ffffff

; Available tokens:
;   %ifname%    [wireless+wired]
;   %local_ip%  [wireless+wired]
;   %local_ip6% [wireless+wired]
;   %essid%     [wireless]
;   %signal%    [wireless]
;   %upspeed%   [wireless+wired]
;   %downspeed% [wireless+wired]
;   %linkspeed% [wired]
; Default: (none)
;label-packetloss = %essid%
;label-packetloss-foreground = ${color.foreground}

; Only applies if <ramp-signal> is used
ramp-signal-0 = "   NET "
ramp-signal-1 = "   NET "
ramp-signal-2 = "   NET "
ramp-signal-foreground = ${color.brown}
ramp-signal-background = ${color.bg}
ramp-signal-font = 1

; Only applies if <animation-packetloss> is used
;;animation-packetloss-0 = ⚠
;;animation-packetloss-0-foreground = #ffa64c
;;animation-packetloss-1 = ⚠
;;animation-packetloss-1-foreground = #000000
; Framerate in milliseconds
;;animation-packetloss-framerate = 500

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/volume]
type = internal/pulseaudio

; Sink to be used, if it exists (find using `pacmd list-sinks`, name field)
; If not, uses default sink
sink = 

; Use PA_VOLUME_UI_MAX (~153%) if true, or PA_VOLUME_NORM (100%) if false
; Default: true
use-ui-max = true

; Interval for volume increase/decrease (in percent points)
; Default: 5
interval = 5

; Available tags:
;   <label-volume> (default)
;   <ramp-volume>
;   <bar-volume>
format-volume = <ramp-volume> <label-volume>
format-volume-background = ${color.bg}

; Available tags:
;   <label-muted> (default)
;   <ramp-volume>
;   <bar-volume>
format-muted = <label-muted>
format-muted-prefix = "  "
format-muted-prefix-font = 2
format-muted-prefix-foreground = ${color.red}
format-muted-background = ${color.bg}

; Available tokens:
;   %percentage% (default)
label-volume = "%percentage%% "
;label-volume-background = ${color.teal}

; Available tokens:
;   %percentage% (default)
label-muted = " Muted "
label-muted-foreground = ${color.red}

; Only applies if <ramp-volume> is used
ramp-volume-0 = "  "
ramp-volume-1 = "  "
ramp-volume-2 = " 󰕾 "
ramp-volume-3 = "  " 
ramp-volume-4 = " 󰝝 "
ramp-volume-font = 2
#ramp-volume-foreground = #0E4D64
;ramp-volume-background = ${color.teal}
ramp-volume-0-foreground = #c4dafa
ramp-volume-1-foreground = #84b6f4
ramp-volume-2-foreground = #4d82bc
ramp-volume-3-foreground = #005187
ramp-volume-4-foreground = #e24162

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/temperature]
type = internal/temperature

; Seconds to sleep between updates
; Default: 1
interval = 1

; Thermal zone to use
; To list all the zone types, run 
; $ for i in /sys/class/thermal/thermal_zone*; do echo "$i: $(<$i/type)"; done
; Default: 0
thermal-zone = 0

; Full path of temperature sysfs path
; Use `sensors` to find preferred temperature source, then run
; $ for i in /sys/class/hwmon/hwmon*/temp*_input; do echo "$(<$(dirname $i)/name): $(cat ${i%_*}_label 2>/dev/null || echo $(basename ${i%_*})) $(readlink -f $i)"; done
; to find path to desired file
; Default reverts to thermal zone setting
;;hwmon-path = /sys/devices/platform/coretemp.0/hwmon/hwmon2/temp1_input

; Threshold temperature to display warning label (in degrees celsius)
; Default: 80
warn-temperature = 70

; Whether or not to show units next to the temperature tokens (°C, °F)
; Default: true
units = true

; Available tags:
;   <label> (default)
;   <ramp>
format = <ramp><label>

; Available tags:
;   <label-warn> (default)
;   <ramp>
format-warn = <ramp><label-warn>

; Available tokens:
;   %temperature% (deprecated)
;   %temperature-c%   (default, temperature in °C)
;   %temperature-f%   (temperature in °F)
label = " %temperature-c% "
label-foreground = ${color.fg}
label-background = ${color.bg}
;label-underline = ${color.YELLOW}

; Available tokens:
;   %temperature% (deprecated)
;   %temperature-c%   (default, temperature in °C)
;   %temperature-f%   (temperature in °F)
label-warn = " %temperature-c% "
label-warn-foreground = ${color.fg}
label-warn-background = ${color.bg}
;label-warn-underline = ${color.YELLOW}

; Requires the <ramp> tag
; The icon selection will range from 0 to `warn-temperature`
; with the current temperature as index.
ramp-0 = "  TEMP"
ramp-1 = "  TEMP"
ramp-2 = "  TEMP"
ramp-3 = "  TEMP"
ramp-4 = "  TEMP"
ramp-5 = "  TEMP"
ramp-6 = "  TEMP"
ramp-7 = "  TEMP"
ramp-8 = "  TEMP"
ramp-9 = "  TEMP"
ramp-font = 3
ramp-foreground = ${color.brown}
ramp-background = ${color.bg}
;ramp-underline = ${color.YELLOW}
;;ramp-foreground = #55

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/keyboard]
type = internal/xkeyboard

; List of indicators to ignore
;blacklist-0 = num lock
blacklist-1 = scroll lock

; Available tags:
;   <label-layout> (default)
;   <label-indicator> (default)
format = <label-layout> <label-indicator>
format-prefix = " "
format-prefix-font = 2
format-prefix-foreground = ${color.PURPLE}
;;format-spacing = 0

; Available tokens:
;   %layout%
;   %name%
;   %number%
; Default: %layout%
label-layout = %name%
;;label-layout-padding = 2
;;label-layout-background = #bc99ed
;;label-layout-foreground = #000

; Available tokens:
;   %name%
; Default: %name%
label-indicator-on = " %name%"
;;label-indicator-padding = 2
label-indicator-on-foreground = ${color.AC}

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/title]
type = internal/xwindow

; Available tags:
;   <label> (default)
format = <label>
format-prefix = " 󰖲 "
format-prefix-font = 4
format-prefix-foreground = ${color.fg}
format-prefix-background = ${color.bg}

; Available tokens:
;   %title%
; Default: %title%
label = %title:11:11:...%
label-foreground = ${color.fg}
label-background = ${color.bg}
;2label-maxlen = 30

; Used instead of label when there is no window title
; Available tokens:
;   None
label-empty = "Desktop           "
label-empty-background = ${color.bg}
label-empty-foreground = ${color.fg}
;label-empty-foreground = ${color.BGA}

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/i3]
type = internal/i3

; Only show workspaces defined on the same output as the bar
;
; Useful if you want to show monitor specific workspaces
; on different bars
;
; Default: false
pin-workspaces = true

; This will split the workspace name on ':'
; Default: false
strip-wsnumbers = true

; Sort the workspaces by index instead of the default
; sorting that groups the workspaces by output
; Default: false
index-sort = true

; Create click handler used to focus workspace
; Default: true
enable-click = true

; Create scroll handlers used to cycle workspaces
; Default: true
enable-scroll = false

; Wrap around when reaching the first/last workspace
; Default: true
wrapping-scroll = false

; Set the scroll cycle direction 
; Default: true
reverse-scroll = false

; Use fuzzy (partial) matching on labels when assigning 
; icons to workspaces
; Example: code;♚ will apply the icon to all workspaces 
; containing 'code' in the label
; Default: false
;;fuzzy-match = true

; ws-icon-[0-9]+ = label;icon
; NOTE: The label needs to match the name of the i3 workspace
ws-icon-0 = 1;
ws-icon-1 = 2;
ws-icon-2 = 3;
ws-icon-3 = 4;
ws-icon-4 = 5;
ws-icon-5 = 6;
ws-icon-6 = 7;
ws-icon-7 = 8;
ws-icon-8 = 9;
;;ws-icon-default = 
; NOTE: You cannot skip icons, e.g. to get a ws-icon-6
; you must also define a ws-icon-5.

; Available tags:
;   <label-state> (default) - gets replaced with <label-(focused|unfocused|visible|urgent)>
;   <label-mode> (default)
format = <label-state> <label-mode>

; Available tokens:
;   %mode%
; Default: %mode%
label-mode = %mode%
label-mode-padding = 1
label-mode-background = ${color.RED}
label-mode-foreground = ${color.BG}

; Available tokens:
;   %name%
;   %icon%
;   %index%
;   %output%
; Default: %icon%  %name%
label-focused = %index%
label-focused-foreground = ${color.AC}
;label-focused-background = ${color.BG}
label-focused-underline = ${color.BG}
label-focused-padding = 1

; Available tokens:
;   %name%
;   %icon%
;   %index%
;   %output%
; Default: %icon%  %name%
label-unfocused = %index%
label-unfocused-foreground = ${color.FG}
;label-unfocused-background = ${color.BG}
label-unfocused-underline = ${color.BG}
label-unfocused-padding = 1

; Available tokens:
;   %name%
;   %icon%
;   %index%
;   %output%
; Default: %icon%  %name%
;label-visible = %index%
label-visible-underline = ${color.BG}
label-visible-padding = 1

; Available tokens:
;   %name%
;   %icon%
;   %index%
;   %output%
; Default: %icon%  %name%
label-urgent = %index%
label-urgent-foreground = ${color.FG}
;label-urgent-background = ${color.RED}
label-urgent-padding = 1

; Separator in between workspaces
;;label-separator = |
;;label-separator-padding = 2
;;label-separator-foreground = #ffb52a


;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_


[module/workspaces]
type = internal/xworkspaces

; Only show workspaces defined on the same output as the bar
;
; Useful if you want to show monitor specific workspaces
; on different bars
;
; Default: false
pin-workspaces = true

; Create click handler used to focus desktop
; Default: true
enable-click = true

; Create scroll handlers used to cycle desktops
; Default: true
enable-scroll = true

; icon-[0-9]+ = <desktop-name>;<icon>
; NOTE: The desktop name needs to match the name configured by the WM
; You can get a list of the defined desktops using:
; $ xprop -root _NET_DESKTOP_NAMES
icon-0 = I;[1]
icon-1 = II;[2] 
icon-2 = III;[3]
; icon-3 = 4;
; icon-4 = 5;
icon-default = 


; Available tags:
;   <label-monitor>
;   <label-state> - gets replaced with <label-(active|urgent|occupied|empty)>
; Default: <label-state>
format = <label-state>
format-padding = 0
format-background = ${color.bg}

; Available tokens:
;   %name%
; Default: %name%
label-monitor = %name%

; Available tokens:
;   %name%
;   %icon%
;   %index%
; Default: %icon%  %name%
;label-active = "  "
label-active = %icon%
label-active-foreground = ${color.brown}
label-active-underline = ${color.fg}
label-active-background = ${color.fg}

; Available tokens:
;   %name%
;   %icon%
;   %index%
; Default: %icon%  %name%
;label-occupied = "  "
label-occupied = %icon%
label-occupied-foreground = ${color.fg}

; Available tokens:
;   %name%
;   %icon%
;   %index%
; Default: %icon%  %name%
;label-urgent = "  "
label-urgent = %icon%
label-urgent-foreground =${color.fg}

; Available tokens:
;   %name%
;   %icon%
;   %index%
; Default: %icon%  %name%
;label-empty = "  "
label-empty = %icon%
label-empty-foreground = ${color.brown}

label-active-padding = 1
label-urgent-padding = 1
label-occupied-padding = 1
label-empty-padding = 1

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/popup-calendar]
type = custom/script
exec = ~/.config/i3/polybar/themes/slim-colorfull/popup-calendar.sh
interval = 60
click-left = ~/.config/i3/polybar/themes/slim-colorfull/popup-calendar.sh --popup &
label = %output%
format = %{F#df773d} 󰨲%{F-}<label>
#label-font = 7
format-background = ${color.bg}
#format-foreground = #ffffff
#label-padding-right = 3pt

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/menu]
type = custom/text

format = " 󰣇 "
#format = "  " 
format-font = 7
format-background = #2f58cd
format-foreground = #e8e2db
;format-padding = 1

click-left = jgmenu_run
;;click-middle = path_to_script
;;click-right = path_to_script

;;scroll-up = path_to_script
;;scroll-down = path_to_script

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/sysmenu]
type = custom/text

format = 
format-font = 2
format-background = ${color.bg}
format-foreground = ${color.red}
format-padding = 0

click-left = ~/.config/i3/polybar/themes/slim-colorfull/powermenu.sh &


[module/popup-powermenu]
type = custom/script
format = "  "
format-font = 7
format-background = #810018
format-foreground = #e8e2db
format-padding = 0
interval = 60
exec = ~/.config/i3/polybar/themes/slim-colorfull/popup-powermenu.sh 
click-left = ~/.config/i3/polybar/themes/slim-colorfull/popup-powermenu.sh --popup &

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/tray]
type = internal/tray
tray-spacing = 5 px
tray-background = ${color.bg}
format-padding = 1
format-foreground = ${color.fg}
format-background = ${color.bg}


;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[glyph]
gleft = ""
gright = ""

[module/left1]
type = custom/text
format-background = ${color.BG2}
format-foreground = ${color.BG1}
format = ${glyph.gleft}
format-font = 6

[module/left2]
type = custom/text
format-background = ${color.BG3}
format-foreground = ${color.BG2}
format = ${glyph.gleft}
format-font = 6

[module/left3]
type = custom/text
format-background = ${color.BG4}
format-foreground = ${color.BG3}
format = ${glyph.gleft}
format-font = 6

[module/left4]
type = custom/text
format-background = ${color.BG}
format-foreground = ${color.BG1}
format = ${glyph.gleft} 
format-font = 6


[module/right1]
type = custom/text
format-background = ${color.BG2}
format-foreground = ${color.BG1}
format = ${glyph.gright} 
format-font = 6

[module/right2]
type = custom/text
format-background = ${color.BG3}
format-foreground = ${color.BG2}
format = ${glyph.gright} 
format-font = 6

[module/right3]
type = custom/text
format-background = ${color.BG4}
format-foreground = ${color.BG3}
format = ${glyph.gright} 
format-font = 6

[module/right4]
type = custom/text
format-background = ${color.BG}
format-foreground = ${color.BG1}
format = ${glyph.gright} 
format-font = 6

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/term]
type = custom/text
format = "  "
format-font = 4
format-foreground = #e8e2db
format-background = #1D1D1D
;format-underline = ${color.bg3}
click-left = kitty &
click-right = ~/.config/i3/scripts/scratchpad &

[module/files]
type = custom/text
format = "  "
format-font = 4
format-foreground = #e8e2db
format-background = #36457A
;format-underline = ${color.bg3}
click-left = thunar &

[module/editor]
type = custom/text
format = "  "
format-font = 4
format-foreground = #e8e2db
format-background = #116a7b
;format-underline = ${color.bg3}
click-left = geany &

[module/firefox]
type = custom/text
format = "  "
format-font = 4
format-foreground = #e8e2db
format-background = #c69749
;format-underline = ${color.bg3}
click-left = exo-open https://www.youtube.com/ &

[module/fake-i3]
type = custom/script
exec = ~/.config/i3/polybar/themes/slim-colorfull/fake-i3.sh
tail = false
interval = 0.2

[module/nightlights]
type = custom/script
exec = ~/.config/i3/polybar/themes/slim-colorfull/nightlight.sh
tail = false
interval = 0.2
click-left = ~/.config/i3/polybar/themes/slim/nightlight.sh brighter
click-right = ~/.config/i3/polybar/themes/slim/nightlight.sh dimmer
click-middle = ~/.config/i3/polybar/themes/slim/nightlight.sh show
format = <label>
format-background = ${color.bg}
format-padding = 1

[module/audiodevice]
type = custom/script
exec = ~/.config/i3/polybar/scripts/venv/bin/python ~/.config/i3/polybar/scripts/audio_manager.py
tail = true
label = %output%
format = <label>
format-background = ${color.bg}
click-left = ~/.config/i3/polybar/scripts/venv/bin/python ~/.config/i3/polybar/scripts/audio_manager.py 1
click-right = ~/.config/i3/polybar/scripts/venv/bin/python ~/.config/i3/polybar/scripts/audio_manager.py -1

[module/netman]
type = custom/text
format = 
format-background = ${color.bg}
format-font = 4
click-left = ~/.config/i3/polybar/scripts/venv/bin/python ~/.config/i3/polybar/scripts/networkmanager_dmenu

[module/bluetooth]
type = custom/script
exec = ~/.config/i3/polybar/scripts/venv/bin/python ~/.config/i3/polybar/scripts/c_bluetooth.py
interval = 2
label = %output%
format = <label>
format-background = ${color.bg}
format-font = 4
click-left = ~/.config/i3/polybar/scripts/venv/bin/python ~/.config/i3/polybar/scripts/bluetooth_menu.py

[module/mikrotik]
type = custom/text
format = " 󱫋 "
format-foreground = #e8e2db
format-background = #0059a9
format-font = 4
click-left = ~/.local/bin/winbox/WinBox


;;	    __________  ______
;;	   / ____/ __ \/ ____/
;;	  / __/ / / / / /_    
;;	 / /___/ /_/ / __/    
;;	/_____/\____/_/       
;;
;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_
