;
;	███╗   ███╗ ██████╗ ██████╗ ██╗   ██╗██╗     ███████╗███████╗
;	████╗ ████║██╔═══██╗██╔══██╗██║   ██║██║     ██╔════╝██╔════╝
;	██╔████╔██║██║   ██║██║  ██║██║   ██║██║     █████╗  ███████╗
;	██║╚██╔╝██║██║   ██║██║  ██║██║   ██║██║     ██╔══╝  ╚════██║
;	██║ ╚═╝ ██║╚██████╔╝██████╔╝╚██████╔╝███████╗███████╗███████║
;	╚═╝     ╚═╝ ╚═════╝ ╚═════╝  ╚═════╝ ╚══════╝╚══════╝╚══════╝
;                                                             
;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/alsa]
type = internal/alsa

; Soundcard to be used
; Usually in the format hw:# where # is the card number
; You can find the different card numbers in `/proc/asound/cards`
master-soundcard = default
speaker-soundcard = default
headphone-soundcard = default

; Name of the master, speaker and headphone mixers
; Use the following command to list available mixer controls:
; $ amixer scontrols | sed -nr "s/.*'([[:alnum:]]+)'.*/\1/p"
; If master, speaker or headphone-soundcard isn't the default, 
; use `amixer -c # scontrols` instead where # is the number 
; of the master, speaker or headphone soundcard respectively
;
; Default: Master
master-mixer = Master

; Optionally define speaker and headphone mixers
; Default: none
;;speaker-mixer = Speaker
; Default: none
;;headphone-mixer = Headphone

; NOTE: This is required if headphone_mixer is defined
; Use the following command to list available device controls
; $ amixer controls | sed -r "/CARD/\!d; s/.*=([0-9]+).*name='([^']+)'.*/printf '%3.0f: %s\n' '\1' '\2'/e" | sort
; You may also need to use `amixer -c # controls` as above for the mixer names
; Default: none
;;headphone-id = 9

; Use volume mapping (similar to amixer -M and alsamixer), where the increase in volume is linear to the ear
; Default: false
;;mapped = true

; Interval for volume increase/decrease (in percent points)
; Default: 5
interval = 5

; Available tags:
;   <label-volume> (default)
;   <ramp-volume>
;   <bar-volume>
;format-volume = <ramp-volume><label-volume><bar-volume>
format-volume = <label-volume><bar-volume>
;format-volume = <ramp-volume><label-volume>
format-volume-background = ${color.gray}
format-volume-padding = 0

; Available tags:
;   <label-muted> (default)
;   <ramp-volume>
;   <bar-volume>
format-muted = <label-muted>
format-muted-prefix = "  "
format-muted-prefix-font = 2
format-muted-prefix-foreground = ${color.fg}
format-muted-prefix-background = ${color.gray}
format-muted-background = ${color.gray}
format-muted-padding = 0

; Available tokens:
;   %percentage% (default)
;label-volume = " %percentage%% "
label-volume = "  "
label-volume-foreground = ${color.green}
label-volume-background = ${color.gray}

format-volume-suffix = " "
format-volume-suffix-background = ${color.gray}


bar-volume-width = 10
;bar-volume-foreground = ${color.CYAN}
bar-volume-foreground-0 = ${color.blue}
bar-volume-foreground-1 = ${color.blue}
bar-volume-foreground-2 = ${color.blue}
bar-volume-foreground-3 = ${color.blue}
bar-volume-foreground-4 = ${color.blue}
bar-volume-foreground-5 = ${color.blue}
bar-volume-foreground-6 = ${color.yellow}
bar-volume-foreground-7 = ${color.yellow}
bar-volume-foreground-8 = ${color.red}
bar-volume-foreground-9 = ${color.red}
;bar-volume-fill = "━"
bar-volume-fill = "█"
;bar-volume-fill = ""
;bar-volume-fill = " "
;bar-volume-fill = ""
;bar-volume-fill-foreground = ${color.purple}
bar-volume-fill-font = 1
bar-volume-fill-background = ${color.gray}

;bar-volume-empty = ""
;bar-volume-empty = "━"
;bar-volume-empty = ""
;bar-volume-empty = " "
bar-volume-empty = "█"
bar-volume-empty-font = 1
bar-volume-empty-foreground = ${color.fg}
bar-volume-empty-background = ${color.gray}
bar-volume-empty-padding-right = 0

;bar-volume-indicator = "雷"
bar-volume-indicator = ""
bar-volume-indicator-foreground = ${color.cyan}
bar-volume-indicator-background = ${color.gray}
bar-volume-indicator-font = 1 
bar-volume-indicator-padding-right = 0

; Available tokens:
;   %percentage% (default
label-muted = " Muted "
label-muted-foreground = ${color.fg}
label-muted-background = ${color.lgray}

; Only applies if <ramp-volume> is used
;ramp-volume-0 = "  "
;ramp-volume-1 = "  "
;ramp-volume-2 = "  "
;ramp-volume-3 = "  "
;ramp-volume-4 = "  "
;ramp-volume-5 = "  "
;ramp-volume-foreground = ${color.bg}
;ramp-volume-background = ${color.purple}
;ramp-volume-font = 1

; If defined, it will replace <ramp-volume> when
; headphones are plugged in to `headphone_control_numid`
; If undefined, <ramp-volume> will be used for both
; Only applies if <ramp-volume> is used
ramp-headphones-0 = " "

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/backlight]
;type = internal/xbacklight
type = internal/backlight

; Use the following command to list available cards:
; $ ls -1 /sys/class/backlight/
;card = intel_backlight
;card = amdgpu_bl0
;card = nv_backlight
card = amdgpu_bl1

enable-scroll = true
use-actual-brightness = true

; Available tags:
;   <label> (default)
;   <ramp>
;   <bar>
;format = <ramp><label>
format = <ramp><bar>
format-background = ${color.bg}
format-padding = 0

format-suffix = " "
format-suffix-background = ${color.gray}

bar-width = 11
bar-foreground-0 = ${color.blue}
bar-foreground-1 = ${color.blue}
bar-foreground-2 = ${color.blue}
bar-foreground-3 = ${color.blue}
bar-foreground-4 = ${color.blue}
bar-foreground-5 = ${color.blue}
bar-foreground-6 = ${color.yellow}
bar-foreground-7 = ${color.yellow}
bar-foreground-8 = ${color.red}
bar-foreground-9 = ${color.red}
bar-foreground-10 = ${color.red}


bar-indicator = "█"
bar-indicator-background = ${color.gray}

bar-fill = "█"
bar-fill-background = ${color.gray}

bar-empty = ""
bar-empty-background = ${color.gray}
;bar-empty-padding-right = 1

; Available tokens:
;   %percentage% (default)
label = " %percentage%%"
label-foreground = ${color.fg}
label-background = ${color.bg}

; Only applies if <ramp> is used
ramp-0 = "  "
ramp-0-foreground = ${color.bg1}
ramp-0-background = ${color.gray}
ramp-1 = "  "
ramp-1-foreground = ${color.fg}
ramp-1-background = ${color.gray}
ramp-2 = "  "
ramp-2-foreground = ${color.ocean}
ramp-2-background = ${color.gray}
ramp-3 = "  "
ramp-3-foreground = ${color.ocean}
ramp-3-background = ${color.gray}
ramp-4 = "  "
ramp-4-foreground = ${color.ocean}
ramp-4-background = ${color.gray}
ramp-background = ${color.bg}
ramp-font = 3 


;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/battery]
type = internal/battery

; This is useful in case the battery never reports 100% charge
full-at = 99

; Use the following command to list batteries and adapters:
; $ ls -1 /sys/class/power_supply/
battery = BAT0
adapter = ADP0

; If an inotify event haven't been reported in this many
; seconds, manually poll for new values.
;
; Needed as a fallback for systems that don't report events
; on sysfs/procfs.
;
; Disable polling by setting the interval to 0.
;
; Default: 5
poll-interval = 5

; see "man date" for details on how to format the time string
; NOTE: if you want to use syntax tags here you need to use %%{...}
; Default: %H:%M:%S
time-format = %H:%M

; Available tags:
;   <label-charging> (default)
;   <bar-capacity>
;   <ramp-capacity>
;   <animation-charging>
format-charging = <animation-charging><label-charging>
format-charging-background = ${color.bluedark}
;format-charging-prefix = ""
;format-charging-prefix-font = 3
;format-charging-prefix-foreground = ${color.yellow}


; Available tags:
;   <label-discharging> (default)
;   <bar-capacity>
;   <ramp-capacity>
;   <animation-discharging>
format-discharging = <label-discharging>
format-discharging-prefix = "  "
format-discharging-prefix-font = 3
format-discharging-prefix-foreground = ${color.bg}
format-discharging-prefix-background = ${color.red}
;format-discharging-background = ${color.bg}

label-discharging-underline = ${color.red}
label-discharging-overline = ${color.red}

format-discharging-suffix = " "
format-discharging-suffix-background = ${color.red}


; Available tags:
;   <label-full> (default)
;   <bar-capacity>
;   <ramp-capacity>
format-full = <label-full>
format-full-prefix = "  "
format-full-prefix-font = 3
format-full-prefix-foreground = ${color.red}
format-full-background = ${color.orange}

label-full-underline = ${color.orange}
label-full-overline = ${color.orange}

format-full-suffix = " "
format-full-suffix-background = ${color.orange}

; Available tokens:
;   %percentage% (default)
;   %time%
;   %consumption% (shows current charge rate in watts)
label-charging = " %percentage:3:3%% "
label-charging-foreground = ${color.bg}
label-charging-background = ${color.blue} 

label-charging-underline = ${color.bluedark}
label-charging-overline = ${color.bluedark}

format-charging-suffix = " "
format-charging-suffix-background = ${color.bluedark}

; Available tokens:
;   %percentage% (default)
;   %time%
;   %consumption% (shows current discharge rate in watts)
label-discharging = " %percentage:3:3%% "
label-discharging-foreground = ${color.bg}
label-discharging-background = ${color.lred} 

; Available tokens:
;   %percentage% (default)
label-full = " %percentage:3:3%% "
label-full-foreground = ${color.bg}
label-full-background = ${color.yellow}

; Only applies if <ramp-capacity> is used
;ramp-capacity-0 = ""
;ramp-capacity-1 = ""
;ramp-capacity-2 = ""
;ramp-capacity-3 = ""
;ramp-capacity-4 = ""
;ramp-capacity-foreground = ${color.green}
;ramp-capacity-background = ${color.bg}
;ramp-capacity-font = 3

; Only applies if <bar-capacity> is used
;bar-capacity-width = 10

; Only applies if <animation-charging> is used
animation-charging-0 = "  "
animation-charging-1 = "  "
animation-charging-2 = "  "
animation-charging-3 = "  "
animation-charging-4 = "  "
animation-charging-foreground = ${color.bg}
animation-charging-background = ${color.bluedark}
animation-charging-font = 1

; Framerate in milliseconds
animation-charging-framerate = 700

; Only applies if <animation-discharging> is used
;;animation-discharging-0 = ${battery.anim0}
;;animation-discharging-1 = ${battery.anim1}

; Framerate in milliseconds
;animation-discharging-framerate = 500

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

;;[module/bspwm]
;;type = internal/bspwm

; Only show workspaces defined on the same output as the bar
; NOTE: The bspwm and XRandR monitor names must match, which they do by default.
; Default: true
;;pin-workspaces = true

; Output mode flags after focused state label
; Default: false
;;inline-mode = false

; Create click handler used to focus workspace
; Default: true
;;enable-click = false

; Create scroll handlers used to cycle workspaces
; Default: true
;;enable-scroll = false

; Set the scroll cycle direction 
; Default: true
;;reverse-scroll = false

; Use fuzzy (partial) matching on labels when assigning 
; icons to workspaces
; Example: code;♚ will apply the icon to all workspaces 
; containing 'code' in the label
; Default: false
;;fuzzy-match = true

; ws-icon-[0-9]+ = label;icon
; Note that the label needs to correspond with the bspwm workspace name
;;ws-icon-0 = code;♚
;;ws-icon-1 = office;♛
;;ws-icon-2 = graphics;♜
;;ws-icon-3 = mail;♝
;;ws-icon-4 = web;♞
;;ws-icon-default = ♟

; Available tags:
;   <label-monitor>
;   <label-state> - gets replaced with <label-(focused|urgent|occupied|empty)>
;   <label-mode> - gets replaced with <label-(monocle|tiled|fullscreen|floating|locked|sticky|private)>
; Default: <label-state>
;;format = <label-state> <label-mode>

; Available tokens:
;   %name%
; Default: %name%
;;label-monitor = %name%

; If any values for label-dimmed-N are defined, the workspace/mode
; colors will get overridden with those values if the monitor is out of focus
; To only override workspaces in a specific state, use:
;   label-dimmed-focused
;   label-dimmed-occupied
;   label-dimmed-urgent
;   label-dimmed-empty
;;label-dimmed-foreground = #555
;;label-dimmed-underline = ${bar/top.background}
;;label-dimmed-focused-background = #f00

; Available tokens:
;   %name%
;   %icon%
;   %index%
; Default: %icon%  %name%
;;label-focused = %icon%
;;label-focused-foreground = #ffffff
;;label-focused-background = #3f3f3f
;;label-focused-underline = #fba922

; Available tokens:
;   %name%
;   %icon%
;   %index%
; Default: %icon%  %name%
;;label-occupied = %icon%
;;label-occupied-underline = #555555

; Available tokens:
;   %name%
;   %icon%
;   %index%
; Default: %icon%  %name%
;;label-urgent = %icon%
;;label-urgent-foreground = #000000
;;label-urgent-background = #bd2c40
;;label-urgent-underline = #9b0a20

; Available tokens:
;   %name%
;   %icon%
;   %index%
; Default: %icon%  %name%
;;label-empty = %icon%
;;label-empty-foreground = #55

; The following labels will be used to indicate the layout/mode
; for the focused workspace. Requires <label-mode>
;
; Available tokens:
;   None
;label-monocle = 
;label-tiled = 
;label-fullscreen = 
;label-floating = 
;label-pseudotiled = P
;label-locked = 
;label-locked-foreground = #bd2c40
;label-sticky = 
;label-sticky-foreground = #fba922
;label-private = 
;label-private-foreground = #bd2c40

; Separator in between workspaces
;;label-separator = |
;;label-separator-padding = 2
;;label-separator-foreground = #ffb52a

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_


[module/date]
type = internal/date

; Seconds to sleep between updates
interval = 60

; See "http://en.cppreference.com/w/cpp/io/manip/put_time" for details on how to format the date string
; NOTE: if you want to use syntax tags here you need to use %%{...}
;; >> Use "man date" command to see available date command <<
;; date = %Y-%m-%d%
;; date = " %a, %d %b %Y" 
;date = "%A, %d %b %Y" 

; Optional time format
;; time = " %I:%M %p"
;time = "%I:%M:%S %p"
time = " %I:%M %p "

; if `date-alt` or `time-alt` is defined, clicking
; the module will toggle between formats
;; date-alt = %A, %d %B %Y
;; time-alt = " %a, %d %b %Y "

; Available tags:
;   <label> (default)
format = <label>
format-prefix = "  "
format-prefix-font = 3
format-prefix-foreground = ${color.bg}
format-prefix-background = ${color.ocean}
format-background = ${color.blue}
format-foreground = ${color.bg}
format-padding = 0

format-suffix = " "
format-suffix-background = ${color.ocean}

; Available tokens:
;   %date%
;   %time%
; Default: %date%
label = %time%
label-underline = ${color.ocean}
label-overline = ${color.ocean}

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/filesystem]
type = internal/fs

; Mountpoints to display
mount-0 = /
;;mount-1 = /home
;;mount-2 = /var

; Seconds to sleep between updates
; Default: 30
interval = 30

; Display fixed precision values
; Default: false
fixed-values = true

; Spacing between entries
; Default: 2
;;spacing = 4

; Available tags:
;   <label-mounted> (default)
;   <bar-free>
;   <bar-used>
;   <ramp-capacity>
format-mounted = <label-mounted>
format-mounted-prefix = 
format-mounted-background = ${color.shade2}
format-mounted-padding = 2

; Available tags:
;   <label-unmounted> (default)
format-unmounted = <label-unmounted>
format-unmounted-prefix = 
format-unmounted-background = ${color.shade2}
format-unmounted-padding = 2

; Available tokens:
;   %mountpoint%
;   %type%
;   %fsname%
;   %percentage_free%
;   %percentage_used%
;   %total%
;   %free%
;   %used%
; Default: %mountpoint% %percentage_free%%
label-mounted = " %free%"

; Available tokens:
;   %mountpoint%
; Default: %mountpoint% is not mounted
label-unmounted = " %mountpoint%: not mounted"

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

;;[module/github]
;;type = internal/github

; Accessing an access token stored in file
;;token = ${file:/path/to/file/containing/github/access.token}

; Accessing an access token stored in an environment variable
;;token = ${env:GITHUB_ACCESS_TOKEN}

; Whether empty notifications should be displayed or not
;;empty-notifications = false

; Number of seconds in between requests
;;interval = 10

; Available tags:
;   <label> (default)
;;format = <label>
;;format-prefix = 

; Available tokens:
;   %notifications% (default)
; Default: Notifications: %notifications%
;;label = %notifications%

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

;;[module/i3]
;;type = internal/i3

; Only show workspaces defined on the same output as the bar
;
; Useful if you want to show monitor specific workspaces
; on different bars
;
; Default: false
;;pin-workspaces = true

; This will split the workspace name on ':'
; Default: false
;;strip-wsnumbers = true

; Sort the workspaces by index instead of the default
; sorting that groups the workspaces by output
; Default: false
;;index-sort = true

; Create click handler used to focus workspace
; Default: true
;;enable-click = false

; Create scroll handlers used to cycle workspaces
; Default: true
;;enable-scroll = false

; Wrap around when reaching the first/last workspace
; Default: true
;;wrapping-scroll = false

; Set the scroll cycle direction 
; Default: true
;;reverse-scroll = false

; Use fuzzy (partial) matching on labels when assigning 
; icons to workspaces
; Example: code;♚ will apply the icon to all workspaces 
; containing 'code' in the label
; Default: false
;;fuzzy-match = true

; ws-icon-[0-9]+ = label;icon
; NOTE: The label needs to match the name of the i3 workspace
;;ws-icon-0 = 1;♚
;;ws-icon-1 = 2;♛
;;ws-icon-2 = 3;♜
;;ws-icon-3 = 4;♝
;;ws-icon-4 = 5;♞
;;ws-icon-default = ♟
; NOTE: You cannot skip icons, e.g. to get a ws-icon-6
; you must also define a ws-icon-5.

; Available tags:
;   <label-state> (default) - gets replaced with <label-(focused|unfocused|visible|urgent)>
;   <label-mode> (default)
;;format = <label-state> <label-mode>

; Available tokens:
;   %mode%
; Default: %mode%
;;label-mode = %mode%
;;label-mode-padding = 2
;;label-mode-background = #e60053

; Available tokens:
;   %name%
;   %icon%
;   %index%
;   %output%
; Default: %icon%  %name%
;;label-focused = %index%
;;label-focused-foreground = #ffffff
;;label-focused-background = #3f3f3f
;;label-focused-underline = #fba922
;;label-focused-padding = 4

; Available tokens:
;   %name%
;   %icon%
;   %index%
;   %output%
; Default: %icon%  %name%
;;label-unfocused = %index%
;;label-unfocused-padding = 4

; Available tokens:
;   %name%
;   %icon%
;   %index%
;   %output%
; Default: %icon%  %name%
;;label-visible = %index%
;;label-visible-underline = #555555
;;label-visible-padding = 4

; Available tokens:
;   %name%
;   %icon%
;   %index%
;   %output%
; Default: %icon%  %name%
;;label-urgent = %index%
;;label-urgent-foreground = #000000
;;label-urgent-background = #bd2c40
;;label-urgent-padding = 4

; Separator in between workspaces
;;label-separator = |
;;label-separator-padding = 2
;;label-separator-foreground = #ffb52a

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/cpu]
type = internal/cpu

; Seconds to sleep between updates
; Default: 1
interval = 2

; Available tags:
;   <label> (default)
;   <bar-load>
;   <ramp-load>
;   <ramp-coreload>
;;format = <label> <ramp-coreload>
format = <label>
format-prefix = "  "
format-prefix-font = 1
format-prefix-foreground = ${color.bg}
format-prefix-background = ${color.purple}
;format-background = ${color.CYAN}
format-padding = 0

format-suffix = " "
format-suffix-background = ${color.purple}

; Available tokens:
;   %percentage% (default) - total cpu load averaged over all cores
;   %percentage-sum% - Cumulative load on all cores
;   %percentage-cores% - load percentage for each core
;   %percentage-core[1-9]% - load percentage for specific core
label = " %percentage:3:3%% "
label-foreground = ${color.bg}
label-background = ${color.lpurple}
label-underline = ${color.purple}
label-overline = ${color.purple}

; Spacing between individual per-core ramps
;;ramp-coreload-spacing = 1
;;ramp-coreload-0 = ${cpu.load0}
;;ramp-coreload-1 = ${cpu.load1}

;;ramp-load-0 = ${cpu.load0}
;;ramp-load-1 = ${cpu.load1}

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_
[module/memory]
type = internal/memory

; Seconds to sleep between updates
; Default: 1
interval = 5

; Available tags:
;   <label> (default)
;   <bar-used>
;   <bar-free>
;   <ramp-used>
;   <ramp-free>
;   <bar-swap-used>
;   <bar-swap-free>
;   <ramp-swap-used>
;   <ramp-swap-free>
format = <label>
format-prefix = "  "
format-prefix-font = 1
format-prefix-foreground = ${color.bg}
format-prefix-background = ${color.indigo}
format-background = ${color.indigolight}
format-foreground = ${color.bg}
format-padding = 0

format-suffix = " "
format-suffix-background = ${color.indigo}

; Available tokens:
;   %percentage_used% (default)
;   %percentage_free%
;   %gb_used%
;   %gb_free%
;   %gb_total%
;   %mb_used%
;   %mb_free%
;   %mb_total%
;   %percentage_swap_used%
;   %percentage_swap_free%
;   %mb_swap_total%
;   %mb_swap_free%
;   %mb_swap_used%
;   %gb_swap_total%
;   %gb_swap_free%
;   %gb_swap_used%

label = " %percentage_used:3:3%% "
label-underline = ${color.indigo}
label-overline = ${color.indigo}

; Only applies if <ramp-used> is used
;;ramp-used-0 = ${memory.used0}
;;ramp-used-1 = ${memory.used1}
;;ramp-used-2 = ${memory.used2}

; Only applies if <ramp-free> is used
;;ramp-free-0 = ${memory.free0}
;;ramp-free-1 = ${memory.free1}
;;ramp-free-2 = ${memory.free2}

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/mpd]
type = internal/mpd

; Host where mpd is running (either ip or domain name)
; Can also be the full path to a unix socket where mpd is running.
;;host = 127.0.0.1
;;port = 6600
;;password = mysecretpassword

; Seconds to sleep between progressbar/song timer sync
; Default: 1
interval = 2

; Available tags:
;   <label-song> (default)
;   <label-time>
;   <bar-progress>
;   <toggle> - gets replaced with <icon-(pause|play)>
;   <toggle-stop> - gets replaced with <icon-(stop|play)>
;   <icon-random>
;   <icon-repeat>
;   <icon-repeatone> (deprecated)
;   <icon-single> - Toggle playing only a single song. Replaces <icon-repeatone>
;   <icon-consume>
;   <icon-prev>
;   <icon-stop>
;   <icon-play>
;   <icon-pause>
;   <icon-next>
;   <icon-seekb>
;   <icon-seekf>
format-online = <label-song> <icon-prev> <toggle> <icon-next>
format-online-prefix = 
format-online-prefix-font = 1
format-online-prefix-background = ${color.shade4}
format-online-foreground = ${color.foreground}

format-playing = ${self.format-online}
format-paused = ${self.format-online}
format-stopped = ${self.format-online}

; Available tags:
;   <label-offline>
format-offline = <label-offline>
format-offline-prefix = 
format-offline-prefix-font = 1
format-offline-prefix-background = ${color.shade4}
format-offline-foreground = ${color.foreground}

; Available tokens:
;   %artist%
;   %album-artist%
;   %album%
;   %date%
;   %title%
; Default: %artist% - %title%
label-song =  " %artist% - %title%"
label-song-maxlen = 25
label-song-ellipsis = true

; Available tokens:
;   %elapsed%
;   %total%
; Default: %elapsed% / %total%
label-time = %elapsed% / %total%

; Available tokens:
;   None
label-offline = " Offline"

; Only applies if <icon-X> is used
icon-play = 契
icon-pause = 
icon-stop = 栗
icon-prev = 玲
icon-next = 怜
icon-seekb = 
icon-seekf = 
icon-random = 
icon-repeat = 
icon-repeatone = 
icon-single = 
icon-consume = 

; Used to display the state of random/repeat/repeatone/single
; Only applies if <icon-[random|repeat|repeatone|single]> is used
toggle-on-foreground = ${color.foreground}
toggle-off-foreground = ${color.background}

; Only applies if <bar-progress> is used
;;bar-progress-width = 45
;;bar-progress-indicator = |
;;bar-progress-fill = ─
;;bar-progress-empty = ─

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

; If you use both a wired and a wireless network, just add 2 module definitions. For example
[module/wired-network]
type = internal/network
interface = wired

; Seconds to sleep between updates
; Default: 1
interval = 1.0

; Test connectivity every Nth update
; A value of 0 disables the feature
; NOTE: Experimental (needs more testing)
; Default: 0
;ping-interval = 3

; @deprecated: Define min width using token specifiers (%downspeed:min% and %upspeed:min%)
; Minimum output width of upload/download rate
; Default: 3
;;udspeed-minwidth = 5

; Accumulate values from all interfaces
; when querying for up/downspeed rate
; Default: false
accumulate-stats = true

; Consider an `UNKNOWN` interface state as up.
; Some devices have an unknown state, even when they're running
; Default: false
unknown-as-up = true

; Available tags:
;   <label-connected> (default)
;   <ramp-signal>
format-connected = <ramp-signal> <label-connected>
format-connected-background = ${color.shade3}
format-connected-padding = 1

; Available tags:
;   <label-disconnected> (default)
format-disconnected = <label-disconnected>
format-disconnected-prefix = "睊 "
format-disconnected-prefix-font = 1
format-disconnected-prefix-foreground = ${color.foreground}
format-disconnected-foreground = ${color.foreground}
format-disconnected-background = ${color.shade3}
format-disconnected-padding = 1

; Available tags:
;   <label-connected> (default)
;   <label-packetloss>
;   <animation-packetloss>
;;format-packetloss = <animation-packetloss> <label-connected>

; Available tokens:
;   %ifname%    [wireless+wired]
;   %local_ip%  [wireless+wired]
;   %local_ip6% [wireless+wired]
;   %essid%     [wireless]
;   %signal%    [wireless]
;   %upspeed%   [wireless+wired]
;   %downspeed% [wireless+wired]
;   %linkspeed% [wired]
; Default: %ifname% %local_ip%
label-connected = "%{A1:networkmanager_dmenu &:}%essid% %{A}"

; Available tokens:
;   %ifname%    [wireless+wired]
; Default: (none)
label-disconnected = "%{A1:networkmanager_dmenu &:}Offline%{A}"
;;label-disconnected-foreground = #66ffffff

; Available tokens:
;   %ifname%    [wireless+wired]
;   %local_ip%  [wireless+wired]
;   %local_ip6% [wireless+wired]
;   %essid%     [wireless]
;   %signal%    [wireless]
;   %upspeed%   [wireless+wired]
;   %downspeed% [wireless+wired]
;   %linkspeed% [wired]
; Default: (none)
;label-packetloss = %essid%
;label-packetloss-foreground = ${color.foreground}

; Only applies if <ramp-signal> is used
ramp-signal-0 = 直
ramp-signal-1 = 直
ramp-signal-2 = 直
ramp-signal-foreground = ${color.foreground}
ramp-signal-font = 1

; Only applies if <animation-packetloss> is used
;;animation-packetloss-0 = ⚠
;;animation-packetloss-0-foreground = #ffa64c
;;animation-packetloss-1 = ⚠
;;animation-packetloss-1-foreground = #000000
; Framerate in milliseconds
;;animation-packetloss-framerate = 500

[module/eth]
type = internal/network
interface-type = wired

interval = 5.0
accumulate-stats = true
unknown-as-up = true
speed-unit = "B"

format-connected = <label-connected>
format-connected-prefix = "  "
format-connected-prefix-foreground = ${color.bg}
format-connected-prefix-background = ${color.orange}
format-connected-prefix-padding = 1 pt

format-connected-suffix = " "
format-connected-suffix-background = ${color.orange}

label-connected = " %{A1:OpenApps --netmanager:}%netspeed:5:5%%{A} "
label-connected-padding-right = 1 pt
label-connected-foreground = ${color.bg}
label-connected-background = ${color.lorange}

label-connected-underline = ${color.orange}
label-connected-overline = ${color.orange}

format-disconnected = <label-disconnected>
format-disconnected-prefix = " 󰈀 "
format-disconnected-foreground = ${color.bg}
format-disconnected-background = ${color.orange}
format-disconnected-prefix-padding = 1 pt

format-disconnected-suffix = " "
format-disconnected-suffix-background = ${color.orange}

label-disconnected = " %{A1:OpenApps --netmanager:}%{A} "
label-disconnected-padding-right = 1 pt
label-disconnected-foreground = ${color.bg}
label-disconnected-background = ${color.lorange}

label-disconnected--underline = ${color.orange}
label-disconnected-overline = ${color.orange}

[module/wlan]
type = internal/network
interface-type = wireless

interval = 5.0
accumulate-stats = true
unknown-as-up = true
speed-unit = "B"

format-connected = <label-connected>
format-connected-prefix = "  "
format-connected-prefix-foreground = ${color.bg}
format-connected-prefix-background = ${color.orange}
format-connected-prefix-padding = 1 pt

format-connected-suffix = " "
format-connected-suffix-background = ${color.orange}

label-connected = " %{A1:OpenApps --netmanager:}%netspeed:5:5%%{A} "
label-connected-padding-right = 1 pt
label-connected-foreground = ${color.bg}
label-connected-background = ${color.lorange}

label-connected-underline = ${color.orange}
label-connected-overline = ${color.orange}

format-disconnected = <label-disconnected>
format-disconnected-prefix = "  "
format-disconnected-foreground = ${color.bg}
format-disconnected-background = ${color.orange}
format-disconnected-prefix-padding = 0 pt

format-disconnected-suffix = " "
format-disconnected-suffix-background = ${color.orange}


label-disconnected = " %{A1:OpenApps --netmanager:} Off %{A} "
label-disconnected-padding-right = 1 pt
label-disconnected-foreground = ${color.bg}
label-disconnected-background = ${color.lorange}

label-disconnected-underline = ${color.orange}
label-disconnected-overline = ${color.orange}

[module/wireless-network]
type = internal/network
interface = wlp3s0

; Seconds to sleep between updates
; Default: 1
interval = 1.0

; Test connectivity every Nth update
; A value of 0 disables the feature
; NOTE: Experimental (needs more testing)
; Default: 0
;ping-interval = 3

; @deprecated: Define min width using token specifiers (%downspeed:min% and %upspeed:min%)
; Minimum output width of upload/download rate
; Default: 3
;;udspeed-minwidth = 5

; Accumulate values from all interfaces
; when querying for up/downspeed rate
; Default: false
accumulate-stats = true

; Consider an `UNKNOWN` interface state as up.
; Some devices have an unknown state, even when they're running
; Default: false
unknown-as-up = true

; Available tags:
;   <label-connected> (default)
;   <ramp-signal>
format-connected = <ramp-signal> <label-connected>
format-connected-background = ${color.shade3}
format-connected-padding = 1

; Available tags:
;   <label-disconnected> (default)
format-disconnected = <label-disconnected>
format-disconnected-prefix = ""
format-disconnected-prefix-font = 1
format-disconnected-prefix-foreground = ${color.foreground}
format-disconnected-foreground = ${color.foreground}
format-disconnected-background = ${color.shade3}
format-disconnected-padding = 1

; Available tags:
;   <label-connected> (default)
;   <label-packetloss>
;   <animation-packetloss>
;;format-packetloss = <animation-packetloss> <label-connected>

; Available tokens:
;   %ifname%    [wireless+wired]
;   %local_ip%  [wireless+wired]
;   %local_ip6% [wireless+wired]
;   %essid%     [wireless]
;   %signal%    [wireless]
;   %upspeed%   [wireless+wired]
;   %downspeed% [wireless+wired]
;   %linkspeed% [wired]
; Default: %ifname% %local_ip%
label-connected = "%{A1:networkmanager_dmenu &:}%essid% %{A}"

; Available tokens:
;   %ifname%    [wireless+wired]
; Default: (none)
label-disconnected = "%{A1:networkmanager_dmenu &:}Offline%{A}"
;;label-disconnected-foreground = #66ffffff

; Available tokens:
;   %ifname%    [wireless+wired]
;   %local_ip%  [wireless+wired]
;   %local_ip6% [wireless+wired]
;   %essid%     [wireless]
;   %signal%    [wireless]
;   %upspeed%   [wireless+wired]
;   %downspeed% [wireless+wired]
;   %linkspeed% [wired]
; Default: (none)
;label-packetloss = %essid%
;label-packetloss-foreground = ${color.foreground}

; Only applies if <ramp-signal> is used
ramp-signal-0 = ""
ramp-signal-1 = ""
ramp-signal-2 = ""
ramp-signal-foreground = ${color.foreground}
ramp-signal-font = 1

; Only applies if <animation-packetloss> is used
;;animation-packetloss-0 = ⚠
;;animation-packetloss-0-foreground = #ffa64c
;;animation-packetloss-1 = ⚠
;;animation-packetloss-1-foreground = #000000
; Framerate in milliseconds
;;animation-packetloss-framerate = 500

; Normal Module

[module/network]
type = internal/network
interface = wlan0

; Seconds to sleep between updates
; Default: 1
interval = 1.0

; Test connectivity every Nth update
; A value of 0 disables the feature
; NOTE: Experimental (needs more testing)
; Default: 0
;ping-interval = 3

; @deprecated: Define min width using token specifiers (%downspeed:min% and %upspeed:min%)
; Minimum output width of upload/download rate
; Default: 3
;;udspeed-minwidth = 5

; Accumulate values from all interfaces
; when querying for up/downspeed rate
; Default: false
accumulate-stats = true

; Consider an `UNKNOWN` interface state as up.
; Some devices have an unknown state, even when they're running
; Default: false
unknown-as-up = true

; Available tags:   <label-connected> (default)
;   <ramp-signal>
format-connected = <ramp-signal><label-connected>
;format-connected = <ramp-signal>
;format-connected-background = ${color.black}
;format-connected-padding = 0

; Available tags:
;   <label-disconnected> (default)
format-disconnected = <label-disconnected>
format-disconnected-prefix = "  "
format-disconnected-prefix-font = 1
format-disconnected-prefix-background = ${color.green}
format-disconnected-prefix-foreground = ${color.bg}
;format-disconnected-foreground = ${color.fg}
;format-disconnected-background = ${color.green}
;format-disconnected-padding = 0

; Available tags:
;   <label-connected> (default)
;   <label-packetloss>
;   <animation-packetloss>
;format-packetloss = <animation-packetloss><label-connected>

; Available tokens:
;   %ifname%    [wireless+wired]
;   %local_ip%  [wireless+wired]
;   %local_ip6% [wireless+wired]
;   %essid%     [wireless]
;   %signal%    [wireless]
;   %upspeed%   [wireless+wired]
;   %downspeed% [wireless+wired]
;   %linkspeed% [wired]
; Default: %ifname% %local_ip%
;label-connected = " %{A1:networkmanager_dmenu &:}%essid%%{A}"
label-connected = "%{A1:networkmanager_dmenu &:}%{A}"
label-connected-foreground = ${color.green}
label-connected-background = ${color.bg}

; Available tokens:
;   %ifname%    [wireless+wired]
; Default: (none)
label-disconnected = "%{A1:networkmanager_dmenu &:}%{A}"
label-disconnected-foreground = ${color.green}
label-disconnected-background = ${color.bg}

; Available tokens:
;   %ifname%    [wireless+wired]
;   %local_ip%  [wireless+wired]
;   %local_ip6% [wireless+wired]
;   %essid%     [wireless]
;   %signal%    [wireless]
;   %upspeed%   [wireless+wired]
;   %downspeed% [wireless+wired]
;   %linkspeed% [wired]
; Default: (none)
;label-packetloss = "%essid%"
;label-packetloss-foreground = ${color.green}
;label-packetloss-background = ${color.bg}

; Only applies if <ramp-signal> is used
ramp-signal-0 = "  "
ramp-signal-1 = "  "
ramp-signal-2 = "  "
ramp-signal-foreground = ${color.bg}
ramp-signal-background = ${color.green}
ramp-signal-font = 1

; Only applies if <animation-packetloss> is used
;animation-packetloss-0 = "⚠"
;animation-packetloss-0-foreground = ${color.green}
;animation-packetloss-1 = "⚠"
;animation-packetloss-1-foreground = ${color.green}
;;Framerate in milliseconds
;animation-packetloss-framerate = 500

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/pulseaudio]
type = internal/pulseaudio

; Sink to be used, if it exists (find using `pacmd list-sinks`, name field)
; If not, uses default sink
sink = alsa_output.pci-0000_03_00.6.analog-stereo

; Use PA_VOLUME_UI_MAX (~153%) if true, or PA_VOLUME_NORM (100%) if false
; Default: true
use-ui-max = false

; Interval for volume increase/decrease (in percent points)
; Default: 5
interval = 5

; Available tags:
;   <label-volume> (default)
;   <ramp-volume>
;   <bar-volume>
format-volume = <ramp-volume> <label-volume>
format-volume-background = ${color.shade3}
format-volume-padding = 1

; Available tags:
;   <label-muted> (default)
;   <ramp-volume>
;   <bar-volume>
format-muted = <label-muted>
format-muted-prefix = 婢
format-muted-background = ${color.shade3}
format-muted-padding = 1

; Available tokens:
;   %percentage% (default)
label-volume = "%percentage%% "

; Available tokens:
;   %percentage% (default
label-muted = " Muted "
label-muted-foreground = ${color.foreground}

; Only applies if <ramp-volume> is used
ramp-volume-0 = 奄
ramp-volume-1 = 奔
ramp-volume-2 = 墳

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/temperature]
type = internal/temperature

; Seconds to sleep between updates
; Default: 1
interval = 3

; Thermal zone to use
; To list all the zone types, run 
; $ for i in /sys/class/thermal/thermal_zone*; do echo "$i: $(<$i/type)"; done
; Default: 0
thermal-zone = 0

; Full path of temperature sysfs path
; Use `sensors` to find preferred temperature source, then run
; $ for i in /sys/class/hwmon/hwmon*/temp*_input; do echo "$(<$(dirname $i)/name): $(cat ${i%_*}_label 2>/dev/null || echo $(basename ${i%_*})) $(readlink -f $i)"; done
; to find path to desired file
; Default reverts to thermal zone setting
;;hwmon-path = /sys/devices/platform/coretemp.0/hwmon/hwmon2/temp1_input
hwmon-path = /sys/devices/pci0000:00/0000:00:18.3/hwmon/hwmon3/temp1_input

; Threshold temperature to display warning label (in degrees celsius)
; Default: 80
warn-temperature = 70

; Whether or not to show units next to the temperature tokens (°C, °F)
; Default: true
units = true

; Available tags:
;   <label> (default)
;   <ramp>
;format = <ramp><label>
format = <ramp><label>
;format-prefix = ""
;format-prefix-font = 3
;format-prefix-foreground = ${color.blue}
;format-prefix-background = ${color.bg}
format-foreground = ${color.bg}
format-background = ${color.magenta}
;format-padding = 1

format-suffix = " "
format-suffix-background = ${color.magenta}

; Available tags:
;   <label-warn> (default)
;   <ramp>
;format-warn = <ramp><label-warn>
format-warn = <ramp><label-warn>
;format-warn-prefix = ""
;format-warn-prefix-font = 3
;format-warn-prefix-foreground = ${color.blue}
;format-warn-prefix-background = ${color.bg}
format-warn-background = ${color.red}
format-warn-foreground = ${color.bg}
;format-warn-padding = 1

format-warn-suffix = " "
format-warn-suffix-background = ${color.magenta}

; Available tokens:
;   %temperature% (deprecated)
;   %temperature-c%   (default, temperature in °C)
;   %temperature-f%   (temperature in °F)
label = " %temperature-c:4:4% "
label-foreground = ${color.bg}
label-background = ${color.pink}

label-underline = ${color.magenta}
label-overline = ${color.magenta}

; Available tokens:
;   %temperature% (deprecated)
;   %temperature-c%   (default, temperature in °C)
;   %temperature-f%   (temperature in °F)
label-warn = " %temperature-c% "
label-warn-foreground = ${color.bg}
label-warn-background = ${color.pink}

label-warn-underline = ${color.magenta}
label-warn-overline = ${color.magenta}


; Requires the <ramp> tag
; The icon selection will range from 0 to `warn-temperature`
; with the current temperature as index.
ramp-0 = "  "
ramp-0-foreground = ${color.bg}
ramp-1 = "  "
ramp-1-foreground = ${color.bg}
ramp-2 = "  "
ramp-2-foreground = ${color.bg}
ramp-3 = "  "
ramp-3-foreground = ${color.bg}
ramp-4 = "  "
ramp-4-foreground = ${color.bg}
ramp-font = 3
;ramp-foreground = ${color.blue}
ramp-background = ${color.magenta}

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/keyboard]
type = internal/xkeyboard

; List of indicators to ignore
blacklist-0 = num lock
blacklist-1 = scroll lock

; Available tags:
;   <label-layout> (default)
;   <label-indicator> (default)
format = <label-layout> <label-indicator>
format-prefix = 
format-background = ${color.shade7}
format-padding = 1

; Available tokens:
;   %layout%
;   %name%
;   %number%
; Default: %layout%
label-layout = " %layout%"

; Available tokens:
;   %name%
; Default: %name%
label-indicator-on = %name%
label-indicator-on-foreground = ${color.foreground}

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/title]
type = internal/xwindow

; Available tags:
;   <label> (default)
format = <label>
format-prefix = "  "
format-prefix-font = 1 
format-background = ${color.gray}
format-foreground = ${color.fg}
format-prefix-underline = ${color.gray}
format-prefix-overline = ${color.gray}
format-prefix-foreground = ${color.indigo}
format-padding = 0

; Available tokens:
;   %title%
; Default: %title%
label = " %title:1:30:...% "
;label-maxlen = 20
label-underline = ${color.gray}
label-overline = ${color.gray}

; Used instead of label when there is no window title
label-empty = " Desktop"
label-empty-foreground = ${color.fg1}
label-empty-background = ${color.bg}

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_
[module/workspaces]
type = internal/xworkspaces

; Only show workspaces defined on the same output as the bar
;
; Useful if you want to show monitor specific workspaces
; on different bars
;
; Default: false
pin-workspaces = true

; Create click handler used to focus desktop
; Default: true
enable-click = true

; Create scroll handlers used to cycle desktops
; Default: true
enable-scroll = true

; icon-[0-9]+ = <desktop-name>;<icon>
; NOTE: The desktop name needs to match the name configured by the WM
; You can get a list of the defined desktops using:
; $ xprop -root _NET_DESKTOP_NAMES
icon-0 = 1;󰖟
icon-1 = 2;
icon-2 = 3;
icon-3 = 4;
icon-4 = 5;
icon-5 = 6;󰎄
icon-6 = 7;
icon-7 = 8;󰑋
icon-8 = 9;
; icon-3 = 4;
; icon-4 = 5;
;icon-default = " " 
icon-font = 1


; Available tags:
;   <label-monitor>
;   <label-state> - gets replaced with <label-(active|urgent|occupied|empty)>
; Default: <label-state>
format = <label-state>
format-padding = 0
;format-background = ${color.YELLOW}

; Available tokens:
;   %name%
; Default: %name%
label-monitor = %name%

; Available tokens:
;   %name%
;   %icon%
;   %index%
; Default: %icon%  %name%
;label-active = ""
label-active =  %icon%
label-active-font = 2
;label-active-underline = ${color.yellow}
;label-active-overline = ${color.yellow}
label-active-background = ${color.gray}
label-active-foreground = ${color.fg}
; ${color.foreground-alt}

; Available tokens:
;   %name%
;   %icon%
;   %index%
; Default: %icon%  %name%
;label-occupied = "  "
;label-occupied = "  "
label-occupied = %icon%
;label-occupied =  %icon%
;label-occupied-foreground = ${color.YELLOW}
label-occupied-font = 1
label-occupied-underline = ${color.gray}
label-occupied-overline = ${color.gray}
label-occupied-foreground = ${color.fg} 
label-occupied-background = ${color.bg}

; Available tokens:
;   %name%
;   %icon%
;   %index%
; Default: %icon%  %name%
;label-urgent = "  "
;label-urgent = %icon%
label-urgent = %index% %icon%
label-urgent-font = 1
label-urgent-foreground = ${color.red}
label-urgent-background = ${color.bg}

; Available tokens:
;
;
;   %name%
;   %icon%
;   %index%
; Default: %icon%  %name%
;label-empty = "  "
;label-empty = "  " 
label-empty = %index% %icon% 
;label-empty = %icon% 
label-empty-font = 1
label-empty-background = ${color.bg}
label-empty-foreground = ${color.fg}

label-active-padding = 1
label-urgent-padding = 0
label-occupied-padding = 1 
label-empty-padding = 0

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

;[module/bspwm]

;type = internal/bspwm
;format = <label-state>
;format-padding = 0
;format-background = ${color.bg}

;pin-workspaces = false  
;inline-mode = false
;enable-scroll = false
;label-empty = ""
;label-empty-foreground = 

;label-focused = ""
;label-focused-underline = ${colors.foreground}
;label-focused-foreground = #ffffff
;label-focused-padding = 1


;label-occupied = 
;label-occupied-underline = ${colors.foreground}
;label-occupied-foreground = #ffffff
;label-occupied-padding = 1

;label-urgent = 
;label-urgent-foreground = #ffffff
;label-urgent-underline = ${colors.foreground}
;label-urgent-padding = 1

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

[module/sep]
type = custom/text
content = " "

[module/sepl]
type = custom/text
content = "|"
content-font = 
content-foreground = ${color.bg1}
content-background = ${color.bg}

[module/links]
type = custom/text
content-padding = 0

[module/telegram]
type = custom/text
exec = /usr/bin/telegram-desktop

click-left = /usr/bin/telegram-desktop &

content = "  "
content-padding = 0
content-font = 5
;content-underline = ${color.blue}
content-foreground = ${color.yellow}
content-background = ${color.bg1}

[module/google]
inherit = module/links
content = " "
content-padding = 0
content-font = 5
;content-underline = ${color.yellow}
content-foreground = ${color.red}
content-background = ${color.bg1}
click-left = exo-open https://www.google.com/ &

[module/github]
inherit = module/links
content = "  "
content-padding = 0
content-font = 5
;content-underline = ${color.green}
content-foreground = ${color.green}
content-background = ${color.bg1}
click-left = exo-open https://www.github.com/ &

;; _-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_-_

